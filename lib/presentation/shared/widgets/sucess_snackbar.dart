import 'package:flutter/material.dart';

class SuccessSnackBar extends SnackBar {
  const SuccessSnackBar({Key? key, required content})
      : super(key: key, content: content, backgroundColor: Colors.green);
}
